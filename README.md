# AI-predictor
AI-Resume-Screener
which uses pdfplumber to extract text from a resume PDF, then uses Groq LLM to summarize and answer questions. The user wants "some numbers medium and unique". We should produce a concise but informative README with key stats, a bit of flavor, and unique aspects of this AI project.

Let's analyze the notebook: It installs pdfplumber, imports Groq, extracts text from "Resume_data_analyst_experienced.pdf", then uses LLM to summarize from recruiter perspective and answer a specific question. The model used is llama-3.3-70b-versatile via Groq API. Project title and description

Key numbers: e.g., PDF pages? (not shown, but we can infer from output), time to extract, model used (70B parameters), API call counts, etc.

Unique aspects: combining pdfplumber with LLM for resume screening, recruiter perspective summarization, QA capability.

Medium length: few paragraphs with bullet points.
